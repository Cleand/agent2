{
  "name": "giftco-responsible-delivery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "start": "node server.mjs", "dev": "node server.mjs", "check": "node --check dist/app.js && node --check dist/catalog.js && node --test tests/catalog.test.mjs" },
  "engines": { "node": ">=20" }
}
