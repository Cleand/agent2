# 디자인·개발 인수인계

## 개선안 반영 내역

| 우선순위 | 홈 위치 | 구현 내용 | 기대 효과 가설 |
|---|---|---|---|
| P0 | 첫 화면 | 슈카 단일 메인 비주얼, 책임배송 뜻, 선물 탐색 CTA | 서비스 이해와 탐색 시작률 향상 |
| P0 | 조건 검색 | 예산·주문 수량·행사일을 함께 입력 | 예산/수량이 맞지 않는 상세 진입 감소 |
| P0 | 상품 카드 | 단가의 기준 수량과 VAT, 최소 주문량 표시 | 결제 직전 가격 오해 감소 |
| P0 | 상품 상세 모달 | 전체 수량별 가격표, 입력 수량 금액, 원본 구매 링크 | 가격 확인 이후 구매 이동률 향상 |
| P0 | 책임배송 | 제작 기간과 고객 수령일 구분, 적용 조건 안내와 상담 | 납기 관련 불확실성 감소 |
| P1 | 탐색 구조 | 목적 타일 → 종류 → 추천 상품 | 상품을 정하지 않은 방문자의 탐색 시작 지원 |
| P1 | 상품 선택 | 3개 비교, 찜 유지, 정렬/인쇄 필터 | 후보 선택과 재방문 지원 |
| P1 | 상담 | 행사 조건 정리 → 이메일/전화 전달 | 상담 준비 부담 감소 |
| P1 | 콘텐츠 | 공식 슈카 영상, 실제 소개 후기 1건, 구분된 선택 가이드 | 브랜드 인지도와 판단 근거 제공 |
| P1 | 모바일 | 축소한 캠페인, 2열 상품, 하단 탐색, 선택 비교 바 | 주요 행동 접근성 개선 |
| P2 | 하단 | 시즌 기획전·혜택·브랜드 연결 | 상품 탐색을 유지하면서 프로모션 노출 |
| P2 | 전체 UI | 일관된 타이포/버튼/여백, 포커스 표시, 모달 키보드 제어 | 정보 위계와 접근성 개선 |

새 기능은 로컬 비교·찜·수량 계산·상담 요약입니다. 실제 배송 가능일 계산, 자동 견적 접수, AI 추천 결과, 보상 자동 지급을 구현했다고 표시하지 않습니다.

## 운영 연결 작업

1. **상품 API:** `catalog.js`의 정적 `products`를 상품 API 결과로 교체. 최소 수량, 판매 단위, VAT, 옵션 비용, 구간 가격, 인쇄/포장, 제작 기간, 판매 상태를 포함합니다. 실제 견적 API가 있다면 중간 수량도 정확한 견적으로 교체합니다.
2. **배송 약속:** 행사일·배송지·상품/수량·인쇄/포장·시안 확정일을 서버에 전달하고 서버가 확정한 수령일만 표시합니다. 행사일은 현재 상담 맥락으로만 사용하며 재고/배송 필터로 위장하지 않습니다.
3. **책임 정책:** `app.js`의 `policy()`와 `sections.js` 책임배송 영역을 운영 정책으로 교체합니다. 대상 주문, 약속 성립 시점, 기한 미준수 처리, 보상 범위, 예외와 문의 절차가 필요합니다. 정책 확인 이전에는 보상액과 성공률 배지를 추가하지 않습니다.
4. **상담 API:** 현재 이메일/전화 인계 구조입니다. 접수 API를 연결할 경우 동의·필수 연락처·검증·접수번호·전송 실패·중복 제출 처리가 필요합니다. 제안/제휴용 공개 이메일을 사용했으므로 실제 주문 상담 수신함을 운영팀이 지정해야 합니다.
5. **계정/찜/장바구니:** 찜은 현재 브라우저 localStorage입니다. 로그인 동기화가 필요하면 계정 API와 연결합니다. 구매/장바구니는 기존 서비스 링크로 연결되며 선택 수량/옵션의 자동 이전은 지원하지 않습니다.
6. **콘텐츠:** 공식 계약 모델 사진으로 교체. 정적 후기/시즌 콘텐츠는 CMS에 연결하고 유효 기간을 관리합니다. 공공기관 증빙과 결제 안내는 실제 지원 정책 확인 후 상세화합니다.
7. **배포:** `dist`는 정적 배포 가능한 코드입니다. 운영 서비스 라우트·도메인으로 외부 링크 교체, 캐시/CSP/보안 헤더 설정, 모바일 실기기 및 접근성 점검 후 배포합니다. 제공 서버는 로컬 검토용입니다.

## 전환 측정 설계

`window`의 `giftco:analytics` CustomEvent를 분석 도구 어댑터에 연결할 수 있습니다. 현재 외부로 분석 데이터를 보내지 않습니다. 이벤트 이름은 `app.js`의 `track()` 호출에서 확인할 수 있습니다.

우선 검증할 5개 변경:

| 변경 | 사용자 테스트 | 측정 |
|---|---|---|
| 슈카·책임배송 캠페인 | 첫 화면을 보고 판매 상품, 약속, 다음 행동을 말하기 | 책임배송 이해율, 탐색 CTA 클릭률 |
| 예산·수량 탐색 | 100개/개당 5천원 조건으로 후보 찾기 | 검색 성공률, 결과 없음 비율, 상세 진입률 |
| 가격 정보 | 50개와 100개 주문 가격 차이 설명하기 | 가격 오해, 상세→기존 구매 페이지 이동률 |
| 후보 비교 | 비슷한 2~3개 상품 중 선택하고 이유 설명하기 | 비교 완료율, 비교 후 구매 이동률 |
| 책임배송 상담 | 정해진 행사일로 일정 문의 준비하기 | 상담 준비 완료율, 실제 접수 전환율 |

실제 결제 완료와 매출 전환은 원본 쇼핑몰의 계측 및 교차 도메인 연결이 있어야 측정 가능합니다. 상담 요약 완료를 문의 접수나 주문으로 집계하지 마세요. A/B 테스트는 트래픽과 기간을 정해 진행하고, 납기 이해도와 문의 품질도 함께 확인합니다.
